import { TicketsSumPipe } from './tickets-sum.pipe';

describe('TicketsSumPipe', () => {
  it('create an instance', () => {
    const pipe = new TicketsSumPipe();
    expect(pipe).toBeTruthy();
  });
});
